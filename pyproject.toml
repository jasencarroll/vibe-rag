[project]
name = "vibe-memory"
version = "0.0.1"
description = "Centralized memory and semantic code search for Mistral Vibe"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.0",
    "asyncpg>=0.29",
    "sqlite-vec>=0.1",
    "httpx>=0.27",
    "watchfiles>=0.21",
    "tree-sitter>=0.22",
    "tree-sitter-languages>=1.10",
    "click>=8.1",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "pytest-httpx>=0.30",
]

[project.scripts]
vibe-memory = "vibe_memory.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/vibe_memory"]

[tool.hatch.build.targets.wheel.force-include]
"src/vibe_memory/templates" = "vibe_memory/templates"
