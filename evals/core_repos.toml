[[repo]]
name = "vibe-rag"
path = "~/dev/vibe-rag"

[[repo.task]]
query = "release automation github workflow publish tag"
query_variants = ["github release publish workflow file and tag automation", "publish workflow for release.published event"]
expected_code = [".github/workflows/publish.yml"]
expected_docs = ["CHANGELOG.md"]
unexpected_code = ["src/vibe_rag/indexing/embedder.py"]
unexpected_docs = []
max_irrelevant_code = 1
max_irrelevant_docs = 1

[[repo.task]]
query = "release procedure and maintainer steps for changelog publish tag workflow"
query_variants = ["operator steps to cut a release and publish notes", "maintainer guide for release tag push and github release"]
expected_docs = ["AGENTS.md", "CHANGELOG.md"]
unexpected_code = ["src/vibe_rag/indexing/embedder.py"]
unexpected_docs = []
max_irrelevant_code = 1
max_irrelevant_docs = 1

[[repo.task]]
query = "session bootstrap hook for codex and mcp startup"
expected_code = ["src/vibe_rag/hook_bridge.py", "src/vibe_rag/cli.py"]
expected_docs = ["README.md", "docs/setup-guide.md"]
unexpected_docs = []
max_irrelevant_code = 1
max_irrelevant_docs = 1

[[repo]]
name = "mistral-vibe"
path = "~/dev/mistral-vibe"

[[repo.task]]
query = "background mcp hook acp session bootstrap trusted folder config"
query_variants = ["background_mcp_hook config and acp session startup with trusted folders", "acp session bootstrap mcp config trust flow"]
expected_code = ["vibe/acp/acp_agent_loop.py", "vibe/core/config/_settings.py", "vibe/cli/entrypoint.py"]
expected_docs = ["README.md", "docs/acp-setup.md"]
unexpected_docs = []
max_irrelevant_code = 1
max_irrelevant_docs = 1

[[repo.task]]
query = "session logging and session loading behavior"
query_variants = ["session logger and session loader behavior", "resume continue load session metadata and messages"]
expected_code = ["vibe/core/session/session_logger.py", "vibe/core/session/session_loader.py", "vibe/acp/acp_agent_loop.py"]
expected_docs = ["README.md"]
unexpected_docs = []
max_irrelevant_code = 1
max_irrelevant_docs = 1

[[repo]]
name = "fda-platform"
path = "~/dev/fda-platform"

[[repo.task]]
query = "backend api auth letters analytics engines billing health endpoints"
expected_code = ["backend/app/main.py"]
expected_docs = ["docs/API.md"]
unexpected_docs = []
max_irrelevant_code = 1
max_irrelevant_docs = 1

[[repo.task]]
query = "warning letter scrape parse classify enrich 483 observation pipeline"
expected_code = ["backend/app/pipeline/cli.py"]
expected_docs = ["docs/PIPELINE.md"]
unexpected_docs = []
max_irrelevant_code = 1
max_irrelevant_docs = 1

[[repo.task]]
query = "mcp tools server typescript route integrations and available tool docs"
expected_code = ["mcp-server/src/index.ts"]
expected_docs = ["docs/MCP-TOOLS.md", "mcp-server/README.md"]
unexpected_docs = []
max_irrelevant_code = 1
max_irrelevant_docs = 1

[[repo]]
name = "agent-os"
path = "~/dev/agent-os"

[[repo.task]]
query = "magic link session auth api routes and cookie login flow"
query_variants = ["auth routes magic link verify session cookie flow", "api auth send magic link me logout session validation"]
expected_code = ["backend/app/routes/auth_routes.py", "backend/app/lib/auth.py", "backend/app/dependencies.py"]
expected_docs = ["docs/docs/api.md", "docs/docs/quickstart.md"]
unexpected_docs = ["FINISH-LINE-PLAN.md", "CLAUDE.md"]
max_irrelevant_code = 1
max_irrelevant_docs = 1

[[repo.task]]
query = "signal bus architecture immutable signals decisions and context injection"
query_variants = ["append only signal store decision lifecycle and signal context injection", "signal bus docs with parser engine and immutability triggers"]
expected_code = ["backend/app/runner/engine.py", "backend/app/runner/parser.py"]
expected_docs = ["docs/docs/signal-bus.md", "README.md"]
unexpected_docs = ["FINISH-LINE-PLAN.md"]
max_irrelevant_code = 1
max_irrelevant_docs = 1

[[repo.task]]
query = "mcp server lifecycle config availability and test endpoint"
query_variants = ["runner mcp server config stdio lifecycle and api mcp test route", "mcp server definitions and api route to test connectivity"]
expected_code = ["backend/app/runner/mcp.py", "backend/app/routes/mcp.py"]
expected_docs = ["docs/docs/mcp-troubleshooting.md", "docs/adr/003-mcp-over-direct-api.md"]
unexpected_docs = ["FINISH-LINE-PLAN.md", "CLAUDE.md"]
max_irrelevant_code = 1
max_irrelevant_docs = 1

[[repo]]
name = "codex"
path = "~/dev/codex"

[[repo.task]]
query = "install build codex cli config toml mcp servers codex home"
query_variants = ["codex install build from source configuration and mcp server setup", "config.toml install docs and codex home settings"]
expected_code = ["codex-rs/config/src/lib.rs"]
expected_docs = ["docs/install.md", "docs/config.md"]
unexpected_docs = []
max_irrelevant_code = 1
max_irrelevant_docs = 1

[[repo.task]]
query = "typescript sdk thread run streamed resume input images and python app server thread api"
query_variants = ["thread runstreamed resume sdk typescript thread ts and python app server api", "codex sdk thread lifecycle run resume events and app server client"]
expected_code = ["sdk/typescript/src/thread.ts", "sdk/python/src/codex_app_server/api.py"]
expected_docs = ["sdk/python/docs/getting-started.md", "sdk/typescript/README.md"]
unexpected_docs = []
max_irrelevant_code = 1
max_irrelevant_docs = 1

[[repo.task]]
query = "mcp server interface shell tool mcp sandbox exec policy approvals"
query_variants = ["codex mcp server json rpc shell tool mcp sandbox exec policy", "mcp interface approvals sandbox and exec policy docs"]
expected_code = ["codex-rs/protocol/src/approvals.rs", "codex-rs/core/src/tools/sandboxing.rs", "codex-rs/mcp-server/src/lib.rs"]
expected_docs = ["codex-rs/docs/codex_mcp_interface.md", "docs/sandbox.md"]
unexpected_docs = []
max_irrelevant_code = 1
max_irrelevant_docs = 1
