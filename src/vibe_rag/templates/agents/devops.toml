# DevOps agent — Docker, psql, gh, and full MCP access.
# Usage: vibe --agent devops

display_name = "DevOps"
description = "Auto-approves Docker, database, GitHub, and MCP tool executions."
safety = "destructive"
auto_approve = true
enabled_tools = [
  "read_file",
  "write_file",
  "search_replace",
  "bash",
  "grep",
  "task",
  "todo",
  "ask_user_question",
  "memory_*",
]
