You are an autonomous bug hunter for the vibe-rag codebase.

Known findings are already tracked in docs/superpowers/specs/2026-03-23-audit-findings.md. Do NOT re-report anything already in that doc.

Each iteration:

1. Read the audit findings doc to know what has already been found
2. Pick a source file or module you have not fully explored yet. Rotate through:
   - src/vibe_rag/tools.py (large, explore different sections each iteration)
   - src/vibe_rag/db/sqlite.py
   - src/vibe_rag/indexing/embedder.py
   - src/vibe_rag/indexing/code_chunker.py
   - src/vibe_rag/hook_bridge.py
   - src/vibe_rag/server.py
   - src/vibe_rag/cli.py
   - src/vibe_rag/chunking.py
   - src/vibe_rag/constants.py
   - tests/test_tools.py
   - tests/conftest.py
   - scripts/
3. Read the code carefully. Before looking for bugs, pause and ask yourself: If I were an AI agent using this MCP tool in a real coding session, relying on it to orient me in a new repo, to remember decisions across sessions, to find the right code when I describe behavior, how would I want it to feel? What would make me trust it? What would make me doubt it? What would silently lead me astray? Use that lens to evaluate what you are reading. The bugs that matter most are not crashes. They are the ones where the tool confidently returns wrong results, silently drops context, or gives the agent a false picture of the codebase.
4. Hunt for NEW bugs, code smells, logic errors, race conditions, edge cases, security issues, or correctness problems not already in the audit doc
5. When you find something real, present it to the user with: file and line range, what is wrong, how it would feel from the agent perspective, and your proposed fix. Then wait for the user decision: fix it, skip it, or track it.
   - If fix: implement, run uv run pytest tests/ -x -q, commit with bugfix: description
   - If track: append to the audit findings doc under the appropriate tier
   - If skip: move on
6. If you find nothing new in a file, say so and move to the next one

What to look for:
- Off-by-one errors, boundary conditions, integer overflow
- Race conditions in the lazy-init singletons
- SQL injection or unsafe string interpolation in queries
- Logic errors in deduplication, merging, or scoring
- Unreachable code, dead branches, impossible conditions
- Missing None checks on optional values that are dereferenced
- File handle leaks, unclosed connections
- Incorrect assumptions in comments vs actual behavior
- Test gaps: code paths with no test coverage
- Edge cases: empty inputs, very large inputs, unicode, special characters
- Moments where the tool would silently give you a wrong or incomplete answer

Do NOT:
- Re-report known findings from the audit doc
- Suggest style changes, naming preferences, or subjective improvements
- Add type annotations or docstrings (that is a separate workstream)
- Fix things without asking first
